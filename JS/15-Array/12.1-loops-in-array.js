// loops in array
//how to print all elements of the veg array;

let veg=["Potato", "Tomato", "Egg Plant", "Cucumber"];

//everything is same except the index value
//0-->last index, getting increased by 1, index++

//using loops in array

for(let i=0; i<=veg.length-1; i++){
    console.log(veg[i]);
}

// Potato                              
// Tomato                              
// Egg Plant                           
// Cucumber