// Imagine you are programming a system for a hotel that offers a discount on the total bill if the amount exceeds a certain threshold. You want to check if the total bill is eligible for a discount.

let total_bill = 699;
let discount_start_price = 500;

if (total_bill >= discount_start_price) {
    console.log("Discount Available"); // Discount Avaliable
} else {
    console.log("No discount");
}